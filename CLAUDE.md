# CLAUDE.md

Claude Code가 이 저장소에서 작업할 때 참고하는 지침.

## Git

```bash
git push -u origin main --no-verify
```

`--no-verify`를 쓴다. pre-push 훅이 이 환경에서 걸려 푸시가 막히므로,
훅을 건너뛰어야 정상적으로 올라간다.

**푸시 전에 반드시 스크럽할 것.** 이 저장소는 공개되어 있고, 원래 특정 조직의
PoC에서 파생됐다.

```bash
cp .scrub-patterns.example .scrub-patterns   # 최초 1회, 실제 값으로 편집
git grep -inE "$(grep -v '^#' .scrub-patterns | grep . | paste -sd'|' -)" -- .
```

하나라도 걸리면 푸시하지 않는다. `.scrub-patterns`는 `.gitignore` 대상이다 —
걸러야 할 문자열 자체가 저장소에 남으면 스크럽의 의미가 없다.

`runs/`, `plans/`, `workloads/`, `*.pem`, `comparisons.local.yaml`도 제외 대상이다.
런 아티팩트에는 대상 DB의 테이블명과 행수가 들어가므로 커밋하면 안 된다.

## 이 도구가 하는 일

빈 SQL Server DB 두 대(as-is / to-be)의 스키마를 조회해 시딩 플랜과 트랜잭션
믹스를 자동 생성하고, 양쪽에 같은 부하를 걸어 리소스 사용량을 비교한다.

```
[사전] 사용자가 스키마만 있는 빈 DB를 양쪽에 준비 (도구 밖)
  1. Schema      조회 + 빈 DB 확인 + 양쪽 스키마 대조
  2. Seed Plan   자동 추천 → UI 수정 → 시딩 → 데이터 동일성 대조
  3. Workload    스키마에서 SQL 초안 → UI 수정
  4. 쿼리 검증    각 SQL 1회 실행 (쓰기는 롤백)
  5. 부하 실행    양쪽에 같은 TPS
  6. 리포트      vCPU 정규화 비교 + 유효성 판정
```

**DB 생성과 스키마 적용은 이 도구가 하지 않는다.** 전제조건이다.

## 설계 원칙 — 코드를 고칠 때 지킬 것

### 1. 조용히 잘못된 데이터가 크래시보다 나쁘다

측정 도구이므로, 부하가 정상적으로 돌면서 엉뚱한 것을 측정하는 것이 가장 위험하다.
사용자는 그럴듯한 숫자를 받고 그걸 근거로 의사결정한다.

구체적으로 경계할 패턴:

| 패턴 | 왜 위험한가 |
|---|---|
| 조회가 0행 반환 | 에러가 아니라 정상 응답 → "성공한 트랜잭션"으로 집계되어 에러율 게이트에 걸리지 않는다 |
| id 범위를 모를 때 상수 대입 | 위와 같은 결과가 된다. 상수 1을 넣지 말고 실패시킬 것 |
| ctx 키에 스키마 생략 | `archive.Invoice`(1000만)와 `sales.Invoice`(120)가 충돌해 좁은 범위가 이긴다 |
| 제약을 끈 상태의 삽입 실패 무시 | 고아 행·중복이 남고 인덱스 선택도가 실제와 달라진다 |
| 목록을 조용히 자르기 | "전체를 덮었다"로 읽힌다. 잘린 개수를 반드시 밝힐 것 |

새 코드가 값을 만들거나 대입할 때는 **"이게 틀렸을 때 사용자가 알 수 있는가"**를
먼저 확인한다. 모른다면 실패시키는 편이 낫다.

### 2. 안전 검사는 쓰기를 수행하는 함수 안쪽에 둔다

API 핸들러에만 두면 CLI나 다른 호출 경로로 우회된다.

- `seeder.seed_plan()` → `require_empty()`
- `coordinator.Run.start()` → id 범위 미해결 시 거부

### 3. 사용자 스키마는 고를 수 없다

`dbo`가 아닌 스키마, 예약어 이름(`[Order]`), 복합 PK/FK, 순환 FK, `xml`·`geography`,
temporal 테이블, 필터 인덱스가 전부 실제로 나온다.

- SQL을 만들 때는 **반드시 `schema/ident.py`의 `quote()`/`qualify()`를 쓴다.**
  raw f-string으로 `dbo.[{table}]`을 쓰면 안 된다
- 새 타입을 다룰 때는 `values.py`의 `GENERATABLE`에 등록한다. 등록하지 않으면
  `value_for()`가 예외를 내고 플랜 단계에서 그 테이블이 제외된다 (의도된 동작)

### 4. 단일 정보원

- 부하 기본값 → `loadgen/presets.py`만
- 비교 쌍·vCPU·게이트 → `comparisons.yaml`만

이 값들을 다른 파일에 복사하면 반드시 갈라지고, 갈라진 측정 조건은 데이터를
조용히 잘못 설명한다. 원본 PoC에서 실제로 그런 일이 있었다.

### 5. 리포트는 답하지 못한 것도 말한다

`analysis._limits()`가 측정의 한계를 자동 생성한다 — CPU가 포화되지 않았으면
무엇이 먼저 막았는지, 인스턴스 세대가 다르면 그 효과가 섞였다는 것 등.
새 한계를 발견하면 여기에 추가한다. 독자가 스스로 채우게 두면 유리한 쪽으로
해석된다.

## 테스트

```bash
.venv/bin/python -m pytest tests/ -q
```

DB가 필요 없다. 가짜 스키마 객체로 FK 정렬·행수 배분·값 전략·워크로드 초안을
검증한다. 버그를 고치면 회귀 테스트를 함께 추가한다 — 테스트 이름과 주석에
"왜 이게 문제인가"를 적을 것.

**성능 측정은 도커로 할 수 없다.** 컨테이너가 먼저 한계에 닿고, 부하기와 DB가
같은 CPU를 나눠 쓰면 측정이 오염된다. 실제 처리량·CPU 비교는 별도 인스턴스가 필요하다.

## 실행

```bash
docker compose up --build          # → http://localhost:8010
LOADGEN_PASSWORD='<pw>' docker compose up   # 외부 노출 시 필수
```

패스워드가 없으면 루프백에서만 동작한다 (fail-closed). 이 동작을 되돌리지 말 것 —
쓰기 부하를 거는 도구가 인증 없이 열려 있으면 안 된다.

## 언어

주석·docstring·UI·문서는 한국어. 커밋 메시지도 한국어로 쓰고, **무엇을 고쳤는지가
아니라 왜 그게 문제였는지**를 적는다.
